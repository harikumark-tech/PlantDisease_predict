In command prompt, Enter this command to install the required libraries to run the project : pip install flask tensorflow pillow numpy scipy gunicorn requests

To run the project open command prompt in MULTI_MEDI_PJT directory and type the command : python app.py 